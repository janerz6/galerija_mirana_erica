<img src="images/Labod_small.png" class="interviewPageTplBg">
<div class="interviewPage">

    <div class="bs-header">

        <img src="images/expand.png" id="back">

        <div class="container">
            <h2>Nova zgradba FRI z umetniškim pridihom</h2>
        </div>
    </div>

    <div class="row" id="itw">
        <div class="col-md-12 col-sm-12">
            <div class="col-md-6 col-sm-6 bback">
                <div class="col-md-5 indexContainer">
                    <ul>
                        {{#each data.parts}}
                            <li><a href="#{{@key}}" >{{this.title}}</a></li>
                        {{/each}}
                    </ul>
                </div>
            </div>
            <div class="col-md-6 col-sm-6 wback">
                <div class="col-md-10 col-md-offset-1 col-xs-10 col-xs-offset-1 interviewContent">
                    <br>
                    {{#each data.parts}}
                        <h5 id="{{@key}}">{{this.title}}</h5>
                        <br/>
                        <p>{{{this.content}}}</p>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>
</div>
